import React from "react";

const SearchNav = () => {
  return (
    <input
      className="bg-white min-w-[100%] p-2 m-3 rounded-lg outline-0"
      placeholder="search..."
      type="search"
      aria-label="Search"
    />
  );
};

export default SearchNav;
